// Single source of truth for the business identity shown on customer-facing
// pages. Centralized here so it isn't re-hardcoded across the app (and so a
// future multi-tenant config has one obvious place to replace).
export const BUSINESS_NAME = "BarberBook";
